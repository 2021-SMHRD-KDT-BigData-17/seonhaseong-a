<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.smhrd.PMnt.mapper.ImformationMapper">
    
 
    <!-- 회원정보 수정 -->
	<update id="imformation" parameterType="kr.smhrd.PMnt.entity.ProUser">
	  update pro_user set userbno=#{userbno}, userpw=#{userpw}, username=#{username}, usercompany=#{usercompany}, useraddr=#{useraddr}, useremail=#{useremail}
	  where userbno=#{userbno}
	</update>

    
    
</mapper> 