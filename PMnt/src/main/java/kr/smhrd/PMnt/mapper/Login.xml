<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.smhrd.PMnt.mapper.Login">
    
    <!-- 로그인한 회원 정보 -->
   	<select id="getLogin" resultType="kr.smhrd.PMnt.entity.ProUser">
	    select * from ProUser where userBno = #{userBno}	    
	   
	</select>
	
    <!-- 회원가입 -->
    <select id="getSign" parameterType="kr.smhrd.PMnt.entity.ProUser" >
        select * from ProUser where userBno = #{userBno}
    </select>
    
</mapper>